1. Open the landing page and click `Features`, `Deployment`, and `Pricing` in the top navbar.
2. Confirm each click smoothly scrolls to the correct section instead of jumping instantly.
3. Verify the section heading remains visible below the sticky header after the scroll completes.
